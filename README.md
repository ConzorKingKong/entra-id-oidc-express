# Microsoft Entra ID OIDC in Express

This project Shows setting up an OIDC flow for Microsoft Entra ID in express

A .env file is required with the following:

```
CLIENT_ID=
CLIENT_SECRET_VALUE=
COOKIE_SECRET=
SECRET=
```

Run `npm start` to start the project